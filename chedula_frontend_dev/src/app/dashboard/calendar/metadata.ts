//need ni para di mag error ang calendar/page.tsx

import { Metadata } from 'next';

export const generateMetadata = (): Metadata => {
  return {
    title: "Calendar - Chedula",
    description: "Manage your bookings and appointments",
  };
};